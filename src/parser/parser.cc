#include "parser.hh"

Parser::Parser(std::string file)
: file(file)
{}

Node* Parser::build_rule(std::string& rule)
{
    Node* root = nullptr;
    //Node* curr = nullptr;

    for (unsigned int i = 0; i < rule.size(); i++)
    {

    }

    return root;
}